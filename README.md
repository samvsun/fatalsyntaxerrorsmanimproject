#(CSE1012) Module 2: Python Operators & Branching 
## FATAL SYNTAX ERRORS (Bulit using Manim Library)

A simple presentation that I built using manim

## Setup & Execution

1. Install system prerequisites (ensure FFmpeg is installed on your system).
2. Install Python dependencies:
   ```bash
   pip install -r requirements.txt
   
3. To render the full video.
   ```bash
   python -m manim -pql main.py Module2FullPresentation
